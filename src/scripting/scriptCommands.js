class ScriptCommand {
    static chat(message) {
        // Send to game log
    }

    static useHeld() {
        // Use selected tool
    }

    static setHotbar(slot) {
        // Switch tool
    }

    static winClick(slot, type) {
        // Click UI
    }

    static closeWindow() {
        // Close modal
    }

    static drop(slot) {
        // Remove item/vehicle
    }

    static startMove(direction) {
        // Move vehicle
    }

    static stopMove(direction) {
        // Stop movement
    }

    static afkOn() {
        // Enable anti-AFK
    }

    static afkOff() {
        // Disable anti-AFK
    }

    static disconnect() {
        // Remove vehicle
    }

    static reconnect() {
        // Re-add vehicle
    }

    static delay(ms) {
        // Wait
    }
}